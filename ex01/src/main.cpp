#include "phonebook.hpp"
#include "contact.hpp"

int	main(void)
{
	Phonebook	phonebook;
	bool		running = true;

	while (running)
	{
		else if ()
		{
			running = 0;
			continue;
		}
	}
	if (running)
	{
		//fazer para o ctrl D
	}
	return (0);
}
